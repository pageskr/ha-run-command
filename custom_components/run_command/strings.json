{
  "title": "Run Command",
  "config": {
    "step": {
      "user": {
        "title": "Configure Run Command Sensor",
        "description": "Execute a command and create a sensor from the result.",
        "data": {
          "name": "Sensor name",
          "command": "Command to execute (Jinja2 template supported)",
          "scan_interval": "Update interval (seconds)",
          "result_type": "Result format",
          "value_template": "Value template (optional)",
          "attribute_templates": "Attribute templates JSON (optional)"
        }
      }
    },
    "error": {
      "unknown": "Unexpected error occurred"
    },
    "abort": {
      "already_configured": "Sensor is already configured"
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Run Command Options",
        "description": "Configure Run Command sensor options.",
        "data": {
          "name": "Sensor name",
          "command": "Command to execute (Jinja2 template supported)",
          "scan_interval": "Update interval (seconds)",
          "result_type": "Result format",
          "value_template": "Value template (optional)",
          "attribute_templates": "Attribute templates JSON (optional)"
        }
      }
    }
  },
  "selector": {
    "result_type": {
      "options": {
        "text": "Text",
        "json": "JSON"
      }
    }
  }
}
